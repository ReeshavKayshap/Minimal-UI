"use client";

import RevealPassword from "@/components/core/RevealPassword";

export default function RevealPasswordPreview() {
  return (
    <div className="flex items-center justify-center bg-neutral-50 w-full min-h-[480px]">
      <RevealPassword />
    </div>
  );
}
